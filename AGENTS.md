# 开发会话入口

- 只要任务会修改代码、功能、桌宠素材或安全策略，开始前必须完整阅读 `docs/DEVELOPMENT_HANDOFF.md`。
- 纯学习、只读了解项目、安装依赖或搭建运行环境时，可以跳过该文档；一旦要改动项目，再先阅读。
- 不得提交本地模型、音频、授权原文、隔离区源图、密钥、运行时设置、`data/user-profile.local.json` 或临时构建目录；以 `.gitignore` 和交接文档中的边界为准。
- 保留用户已有改动。提交前运行交接文档中的最小验收，并审查暂存区。
