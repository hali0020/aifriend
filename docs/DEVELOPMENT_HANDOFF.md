# 开发交接与未完成事项

更新时间：2026-08-28

本文是后续开发会话的入口。修改代码、功能、安全策略或桌宠素材前必须先完整阅读；纯学习、只读查看、安装依赖或搭建运行环境时可以跳过。

## 当前可用基线

- Web 服务与 Electron 桌宠已经接入同一个本地 Agent。
- 默认语言模型为本机 Ollama；当前机器最近一次运行使用 `qwen2.5:3b`。本地不可用时不会自动上传到云端。
- 语音模型按需启动：只有进入语音模式或发送语音时才加载，结束后释放。
- 普通对话和游戏分析均有输入、完整输出两道本地文本安全检查。可选 OpenAI Moderation 默认关闭，且游戏画面始终禁止远程审核。
- Electron renderer 启用 sandbox、context isolation、有限 IPC、来源校验、导航/新窗口阻断和临时本地端口。
- 游戏陪玩可由用户明确选择窗口并离散截帧，本地视觉模型分析；不读取游戏内存、不控制键鼠、不自动上传画面。
- 桌宠静态素材已验收并编入目录 82 张（编号 01–82）；动画共 84 帧。最近 12 张可在桌宠中轮换，约 3.5 秒后恢复待机/边缘姿态。
- 人物提示词使用“克里斯提娜/牧濑红莉西”的理性、证据意识、反问、克制吐槽和嘴硬式关心，不冒充真人或官方产品。
- 公开仓库使用重建后的净化历史；真实用户资料集中在被忽略的 `data/user-profile.local.json`，仓库只保留空值模板。

## 收尾时刻意暂停的事项

用户因额度将尽要求停止继续扩展。以下项目不是故障，而是明确留给下一次开发：

1. 桌宠静态动作 83–88 尚未生成，也未写入 `catalog.json`：
   - 83：困倦揉一只眼睛；另一只眼半睁，不哭、不打哈欠。
   - 84：两指搭在另一只手腕内侧检查脉搏；不要手表。
   - 85：拇指与食指轻捏鼻梁、闭眼无奈；不要做成捂脸或哭泣。
   - 86：一只手臂横过胸前，另一只手拉住肘部做肩部拉伸；不要做成抱臂。
   - 87：单手在胸前向自己弯指招手；不要做成挥手、指人或双臂欢迎。
   - 88：双手在嘴边拢成扩音状喊话；不要做成捂嘴或受惊。
2. 连续自动生成更多动作已经暂停。若恢复，仍须逐张生成、透明化、主代理视觉验收、隔离源图，再同步目录；不要一次批量放行。
3. Electron 已通过自动测试，但仍需要在用户自己的 Windows PowerShell 中做一次真实 GUI 长时间运行测试，重点观察此前出现过的 `electron.exe` 原生崩溃是否复现。
4. 游戏陪玩仍为实验功能，需要在实际游戏中检查截帧延迟、视觉模型显存释放、窗口切换和无剧透体验。
5. 本地安全层目前只检查文本和媒体结构；本地图像语义审核尚未实现。普通聊天只有在用户显式开启时才可调用官方 OpenAI Moderation，游戏画面永不远传。
6. 角色原作文本/音频训练数据未加入仓库。只有用户合法持有并确认授权的素材才可放入本地 `data/character-sources/`；隔离区内容不参与构建且不提交。
7. 音色克隆按用户要求暂缓。当前朗读仍使用系统可用语音，不应宣称是牧濑红莉西官方音色。
8. 尚未制作 Electron 安装包、签名、自动更新与崩溃转储收集；如要发布给其他机器，需要单独设计隐私和签名流程。

## 下一次开发的固定起点

1. 先查看 `git status --short --branch`，保留已有未提交内容。
2. 阅读本文和 `README.md`，确认本次只处理一个明确范围。
3. 运行 `npm ci`（仅首次或锁文件变化时）。
4. 修改前先运行相关专项测试；修改后至少运行 `npm test` 与 `npm run check`。
5. 新增桌宠静态 PNG 时：
   - 使用 `public/desktop-pet-assets/makise-kurisu-chibi-01-joyful-wave.png` 作为风格母版。
   - 最终文件必须为真实 RGBA 静态 PNG、透明背景/四角、连续编号、单文件不超过 16MB。
   - 绿幕、预览和失败稿只放 `data/quarantine/imagegen/`，不得提交。
   - 逐张运行 `python scripts/validate_pet_asset.py <文件>` 并人工查看透明边缘、手指、肢体和动作语义。
   - 最后运行 `npm run catalog:sync`、`npm run test:catalog` 和 `npm run test:pet`。
6. 在真实页面验证最近 12 张轮换顺序、待机恢复，并确认浏览器 console 无 error/warning。
7. 提交前运行 `git diff --check`，检查暂存区，不提交 `.env`、`data/user-profile.local.json`、模型、音频、授权原文、`data/quarantine/`、`work/`、`tmp/` 或运行时状态。

## 不可破坏的安全边界

- 不得把“本地优先”悄悄改成自动云端回退；远程处理必须由用户明确选择。
- 游戏截图及其分析始终 `allowRemote: false`。
- `data/user-profile.local.json` 与 `data/memory.json` 只允许进入本地普通聊天；云端模式与游戏陪玩不得读取或发送它们，前端与 Electron IPC 不得暴露原文。
- 被安全策略判为 `support` 或 `block` 的用户原文不写历史；敏感信息只保存脱敏版本。
- 模型完整输出通过输出检查后才能展示，不能为追求流式观感而先泄露再拦截。
- Electron 不开放通用文件、shell、任意 URL 或任意 IPC 能力；所有 renderer 输入必须白名单校验。
- 桌宠只覆盖显示，不向游戏或其他进程注入代码。
- 不抓取、打包或提交未经许可的完整原作脚本、Alarm/导航语音包或社区转载资源。
